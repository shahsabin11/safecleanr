% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_join.R
\name{safe_join}
\alias{safe_join}
\title{Safely Join Two Data Frame
Performs a left join with safety checks for:
duplicate keys, missing join columns, and unmatched rows}
\usage{
safe_join(df1, df2, by)
}
\arguments{
\item{df1}{First data frame}

\item{df2}{Second data frame}

\item{by}{Column name used for joining (string)}
}
\value{
A safely joined data frame
}
\description{
Safely Join Two Data Frame
Performs a left join with safety checks for:
duplicate keys, missing join columns, and unmatched rows
}
\examples{
df1 <- data.frame(id = c(1, 2, 3), name = c("A", "B", "C"))
df2 <- data.frame(id = c(1, 2, 4), score = c(90, 85, 70))

safe_join(df1, df2, by = "id")

}
